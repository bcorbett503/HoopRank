import 'package:flutter/material.dart';

class DiscoverCourtsScreen extends StatelessWidget {
  const DiscoverCourtsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Discover Courts')),
      body: const Center(child: Text('Map placeholder — integrate Google Maps SDK.')),
    );
  }
}
